%h1 Words List
%p
  We have 
  = @words.count
  words in database
%table
  - @words.each do |word|
    %tr
      %td= word.language
      %td= word.value
      %td= link_to "Edit", edit_word_path(word), class: "btn btn-warning"
      %td= link_to "Show", word_path(word), class: "btn btn-info"
      %td= link_to "delete", word_path(word), method: :delete, data: { confirm: "Are you Sure?"}, class: "btn btn-danger" 
