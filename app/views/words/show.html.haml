%h1 Word
%p
  = @word.content
%p 
  = @word.language.name
- if @word.translations.any?
  %h3 Translations
  %p= @word.translations.pluck(:content).join(',')
%p 
  = @random_word.first.content


= link_to "Back", words_path
