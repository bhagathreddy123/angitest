<% if @employee.errors.any? %>
$("#errorArea").html("<%= escape_javascript render('error', employee: @employee) %>");
<% else %>
$("#employeeRow_<%= @employee.id %>").replaceWith("<%= escape_javascript render('employee_row', employee: @employee)%>");
$(".close-modal").click();
<% end %>